<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Employee Application</title>
</head>
<body>
<table border="1">
    <thead>
    <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Role</th>
    </tr>
    </thead>
    <tbody>
    <tr th:each="employee : ${employees}">
        <td th:text="${employee.firstName}"></td>
        <td th:text="${employee.lastName}"></td>
        <td th:text="${employee.email}"></td>
        <td th:text="${employee.role}"></td>
        <td>
            <a class="btn btn-primary" th:if="${employee.role} == 'ADMIN'">Add New Employee</a>
            <a class="btn btn-primary" th:unless="${employee.role} == 'ADMIN'">View Employee</a>
        </td>
    </tr>
    </tbody>
</table>
</body>
</html>